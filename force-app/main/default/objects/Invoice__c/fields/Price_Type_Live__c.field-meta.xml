<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Price_Type_Live__c</fullName>
    <description>Live formula pulling Order__r.Price_Type__c directly — always matches the Order&apos;s current value and can never go stale, unlike the legacy Price_Type__c field on this object (which is a one-time snapshot taken at invoice creation). Prefer this field for any new reports, list views, or code.</description>
    <externalId>false</externalId>
    <formula>TEXT(Order__r.Price_Type__c)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Price Book Type</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
